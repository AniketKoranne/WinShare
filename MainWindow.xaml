<Window x:Class="WinShare.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="WinShare Pro – Premium Local Data Transfer"
        Height="700" Width="960"
        MinHeight="640" MinWidth="880"
        WindowStartupLocation="CenterScreen"
        Background="#F8FAFC">

    <Window.Resources>
        <Style x:Key="Card" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="CornerRadius" Value="16"/>
            <Setter Property="Padding" Value="20"/>
            <Setter Property="BorderBrush" Value="#E2E8F0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect BlurRadius="16" ShadowDepth="4" Opacity="0.04" Color="#0F172A"/>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="BtnPrimary" TargetType="Button">
            <Setter Property="Background" Value="#2563EB"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Height" Value="38"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="10" Padding="14,0">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#1D4ED8"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#1E40AF"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="#CBD5E1"/>
                                <Setter Property="Foreground" Value="#94A3B8"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="BtnDanger" TargetType="Button" BasedOn="{StaticResource BtnPrimary}">
            <Setter Property="Background" Value="#EF4444"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#DC2626"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="BtnSecondary" TargetType="Button" BasedOn="{StaticResource BtnPrimary}">
            <Setter Property="Background" Value="#10B981"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#059669"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="LogItem" TargetType="TextBlock">
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Foreground" Value="#334155"/>
            <Setter Property="Margin" Value="0,4"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
        </Style>
    </Window.Resources>

    <Grid Margin="24">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="280"/>
            <ColumnDefinition Width="20"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="20"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="3" Style="{StaticResource Card}" Padding="24,16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="📡" FontSize="28" VerticalAlignment="Center" Margin="0,0,12,0"/>
                    <StackPanel>
                        <TextBlock Text="WinShare Pro" FontSize="22" FontWeight="Bold" Foreground="#0F172A" />
                        <TextBlock Text="High-Performance Local Network Storage Engine" FontSize="12" Foreground="#64748B" FontWeight="Medium"/>
                    </StackPanel>
                </StackPanel>
                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <Ellipse x:Name="StatusDot" Width="10" Height="10" Fill="#EF4444" Margin="0,0,8,0" VerticalAlignment="Center"/>
                    <TextBlock x:Name="StatusLabel" Text="Stopped" FontWeight="Bold" Foreground="#EF4444" VerticalAlignment="Center" FontSize="13"/>
                </StackPanel>
            </Grid>
        </Border>

        <ScrollViewer Grid.Row="2" Grid.Column="0" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <Border Style="{StaticResource Card}" Margin="0,0,0,16">
                    <StackPanel>
                        <TextBlock Text="Quick Connection Gateway" FontWeight="Bold" Foreground="#1E293B" FontSize="13" Margin="0,0,0,14"/>
                        <Border x:Name="QrBorder" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="12" Width="210" Height="210" HorizontalAlignment="Center" Background="#F8FAFC">
                            <Grid>
                                <TextBlock x:Name="QrPlaceholder" Text="Start transmission engine&#10;to bind QR target" HorizontalAlignment="Center" VerticalAlignment="Center" TextAlignment="Center" Foreground="#94A3B8" FontSize="12" LineHeight="18"/>
                                <Image x:Name="QrCodeImage" Stretch="Uniform" Margin="12"/>
                            </Grid>
                        </Border>
                        <TextBlock x:Name="IpLabel" Text="—" HorizontalAlignment="Center" Margin="0,14,0,0" FontSize="13" Foreground="#2563EB" FontWeight="Bold" TextWrapping="Wrap" TextAlignment="Center"/>
                        <Button x:Name="CopyUrlBtn" Content="📋 Copy Entry Endpoint Link" Style="{StaticResource BtnSecondary}" Margin="0,12,0,0" Click="CopyUrl_Click" IsEnabled="False" Height="34" FontSize="11"/>
                    </StackPanel>
                </Border>

                <Border Style="{StaticResource Card}" Margin="0,0,0,16">
                    <StackPanel>
                        <TextBlock Text="Engine Network Target" FontWeight="Bold" Foreground="#1E293B" FontSize="13" Margin="0,0,0,14"/>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="50"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="Port IP" VerticalAlignment="Center" FontSize="12" Foreground="#64748B" FontWeight="Medium"/>
                            <TextBox x:Name="PortBox" Grid.Column="1" Text="8080" FontSize="13" FontWeight="SemiBold" Height="34" Padding="10,4" VerticalContentAlignment="Center" BorderBrush="#CBD5E1" BorderThickness="1" Foreground="#334155">
                                <TextBox.Style>
                                    <Style TargetType="TextBox">
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="TextBox">
                                                    <Border Background="White" CornerRadius="8" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                                                        <ScrollViewer x:Name="PART_ContentHost" Margin="2,0"/>
                                                    </Border>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                    </Style>
                                </TextBox.Style>
                            </TextBox>
                        </Grid>
                        <Grid Margin="0,14,0,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="10"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Button x:Name="StartBtn" Content="▶ Run Server" Style="{StaticResource BtnPrimary}" Click="StartServer_Click" Height="36"/>
                            <Button x:Name="StopBtn" Grid.Column="2" Content="■ Stop" Style="{StaticResource BtnDanger}" Click="StopServer_Click" IsEnabled="False" Height="36"/>
                        </Grid>
                    </StackPanel>
                </Border>

                <Border Style="{StaticResource Card}">
                    <StackPanel>
                        <TextBlock Text="Provision Outbound Packages" FontWeight="Bold" Foreground="#1E293B" FontSize="13" Margin="0,0,0,14"/>
                        <Button x:Name="AddFilesBtn" Content="➕ Stage Files for Mobile Client" Style="{StaticResource BtnSecondary}" Click="AddFiles_Click" IsEnabled="False" Height="36"/>
                        <ListBox x:Name="SharedFilesList" Margin="0,10,0,0" Height="110" BorderBrush="#E2E8F0" BorderThickness="1" Background="#F8FAFC" FontSize="12" Foreground="#334155" Padding="4">
                            <ListBox.ItemContainerStyle>
                                <Style TargetType="ListBoxItem">
                                    <Setter Property="Padding" Value="6,4"/>
                                    <Setter Property="Margin" Value="0,2"/>
                                    <Setter Property="Background" Value="White"/>
                                    <Setter Property="BorderBrush" Value="#E2E8F0"/>
                                    <Setter Property="BorderThickness" Value="1"/>
                                    <Style.Resources>
                                        <Style TargetType="Border">
                                            <Setter Property="CornerRadius" Value="6"/>
                                        </Style>
                                    </Style.Resources>
                                </Style>
                            </ListBox.ItemContainerStyle>
                        </ListBox>
                        <Button x:Name="ClearFilesBtn" Content="🗑 Flush Allocation List" Style="{StaticResource BtnPrimary}" Background="#64748B" Margin="0,8,0,0" Height="32" FontSize="11" Click="ClearFiles_Click"/>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>

        <Grid Grid.Row="2" Grid.Column="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="20"/>
                <RowDefinition Height="140"/>
            </Grid.RowDefinitions>

            <Border Style="{StaticResource Card}" Grid.Row="0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <Grid Margin="0,0,0,14">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="Application Activity Ledger" FontWeight="Bold" Foreground="#0F172A" FontSize="14" VerticalAlignment="Center"/>
                        <Button Grid.Column="1" Content="Clear Console" Style="{StaticResource BtnPrimary}" Background="#94A3B8" Height="28" FontSize="11" Click="ClearLog_Click" Padding="12,0"/>
                    </Grid>
                    <Border Grid.Row="1" Background="#F8FAFC" CornerRadius="10" BorderBrush="#E2E8F0" BorderThickness="1" Padding="12">
                        <ScrollViewer x:Name="LogScrollViewer" VerticalScrollBarVisibility="Auto">
                            <StackPanel x:Name="LogPanel"/>
                        </ScrollViewer>
                    </Border>
                </Grid>
            </Border>

            <Border Style="{StaticResource Card}" Grid.Row="2">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <TextBlock Text="Performance Metrics Diagnostics" FontWeight="Bold" Foreground="#0F172A" FontSize="13" Margin="0,0,0,14"/>
                    <UniformGrid Grid.Row="1" Columns="3" Rows="1">
                        <Border Background="#EFF6FF" CornerRadius="10" Margin="0,0,10,0" Padding="12,8" BorderBrush="#DBEAFE" BorderThickness="1">
                            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                                <TextBlock Text="📥 Total Inbound" FontSize="11" Foreground="#2563EB" HorizontalAlignment="Center" FontWeight="Bold"/>
                                <TextBlock x:Name="ReceivedCount" Text="0 files" FontSize="18" FontWeight="Bold" Foreground="#1E40AF" HorizontalAlignment="Center" Margin="0,4,0,0"/>
                            </StackPanel>
                        </Border>
                        <Border Background="#ECFDF5" CornerRadius="10" Margin="0,0,10,0" Padding="12,8" BorderBrush="#D1FAE5" BorderThickness="1">
                            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                                <TextBlock Text="📤 Outbound Delivered" FontSize="11" Foreground="#10B981" HorizontalAlignment="Center" FontWeight="Bold"/>
                                <TextBlock x:Name="SentCount" Text="0 files" FontSize="18" FontWeight="Bold" Foreground="#065F46" HorizontalAlignment="Center" Margin="0,4,0,0"/>
                            </StackPanel>
                        </Border>
                        <Border Background="#FFF7ED" CornerRadius="10" Padding="12,8" BorderBrush="#FFEDD5" BorderThickness="1">
                            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                                <TextBlock Text="📁 Host Target Node" FontSize="11" Foreground="#D97706" HorizontalAlignment="Center" FontWeight="Bold"/>
                                <TextBlock x:Name="SavePathLabel" Text="Desktop" FontSize="13" FontWeight="Bold" Foreground="#9A3412" HorizontalAlignment="Center" TextWrapping="Wrap" TextAlignment="Center" Margin="0,4,0,0"/>
                            </StackPanel>
                        </Border>
                    </UniformGrid>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</Window>